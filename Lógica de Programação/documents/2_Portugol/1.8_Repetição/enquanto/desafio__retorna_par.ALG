Algoritmo "Retorna par"

Var 
numero, contador: inteiro

Inicio
   contador := 0
   numero := 102

   enquanto contador < numero faca
      se (numero % 2 = 0) entao
         escreval(contador)
         contador := contador + 2
      fimse
   fimenquanto
Fimalgoritmo